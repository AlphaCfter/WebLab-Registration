$(document).ready(function () {
    // Optional: Add dynamic functionality, such as form validation or styling
    $("form").on("submit", function () {
        alert("Form submitted successfully!");
    });
});
